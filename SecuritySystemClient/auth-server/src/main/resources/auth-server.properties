# AuthServer Configuration
# MQTT Broker Settings
mqtt.broker=tcp://localhost:1883
mqtt.topic.auth.request=home/arduino/auth_requests
mqtt.topic.auth.response=home/arduino/auth_response

# Database Settings - Updated to match existing schema
database.url=jdbc:mysql://localhost:3306/securitysystem
database.username=root
database.password=root

# Authentication Settings
auth.max.retries=5
auth.retry.timeout.seconds=1
